/**
 * A placeholder component for a Metric Card.
 * This component is designed to display key performance indicators or single metrics
 * with a clear label and value.
 * @returns {JSX.Element} The MetricCard component.
 */
export function MetricCard() {
  return (
    <div
      style={{
        border: '1px solid #e0e0e0',
        padding: '1rem',
        borderRadius: '0.5rem',
        backgroundColor: '#f9f9f9',
        textAlign: 'center',
        color: '#757575',
      }}
    >
      Metric Card Placeholder
    </div>
  );
}
